included:
    - Sources
    - Tests
custom_rules:
  double_space:
    include: "*.swift"
    name: "Double space" 
    regex: "([a-z,A-Z] \s+)" 
    message: "Double space between keywords" 
    match_kinds: keyword 
    severity: warning 
